def square_array(array)
  new_array = square_array.each |array ** 2 |
  new_array
end

# The Google says that to return a NEW array, I'd have to use .map, but that's not part of the requirements so..

